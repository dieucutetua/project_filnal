/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js,jsx,ts,tsx}", // Đường dẫn tới file HTML, JS, JSX
  ],
  theme: {
    extend: {}, // Bạn có thể thêm các tùy chỉnh nếu cần
  },
  plugins: [], // Thêm plugins nếu cần
};
